import React from 'react';
import * as S from './Footer.styled';

function Footer(props) {
  return <S.Footer>{/* A footer has been requested, but no content for it provided. */}</S.Footer>;
}

export default Footer;
